#ifndef SCREEN_H
#define SCREEN_H

#include <string>
#include <vector>

class Screen {
public:
    static int width;
    static int height;
    static std::string title;
    static std::vector<float> color;
};

#endif
